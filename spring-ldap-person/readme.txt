To run this sample project, run "ant dist" and copy target/artifacts/war/ldap-person.war
to the webapps directory of your tomcat server. It runs Apache-DS directory server
setup with some initial data internally.